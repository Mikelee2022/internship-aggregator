export default {
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    darkMode: 'selector', // or 'class' for older versions, 'selector' is explicit for v3.4+
    theme: {
        extend: {},
    },
    plugins: [],
}
